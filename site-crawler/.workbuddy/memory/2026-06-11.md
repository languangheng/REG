# 2026-06-11 工作日志

## browser_act 功能封装（F1-F4）
- F1 screenshot: 新增 `screenshot(path, full_page)` — 已提交 `6d3bac5`
- F2 back/forward/reload: 新增 `back()`, `forward()`, `reload()` — 已提交 `5e3cf23`
- F3 input/keys: 新增 `input_text(index, text)`, `press_key(key_combo)` — 已提交 `040fc9e`
- F4 wait_selector: 新增 `wait_selector(selector, state, timeout)` — 已验证但**未提交**（browser_act.py 有未提交的 F4 改动）

## 项目文件整理
- 将 `_archive/` 整体移入 `tmp/_archive/`
- 根目录 25 个临时文件（debug_*/fix_*/test_*/日志/JSON/HAR）全部移入 `tmp/_archive/`
- `.gitignore` 精简为：`__pycache__/`, `*.pyc`, `*.pyo`, `tmp/`
- `WORKFLOW.md` 新增"测试与临时文件规范"条款
- 后续测试脚本统一放 `tmp/` 目录
- 已提交 `ee070b7`

## BUG1: HAR 录制后配置未刷新到列表
- **根因**: wizard_har.html 用 `window.parent.postMessage()` 发通知，但弹窗通过 `window.open()` 打开，`window.parent` 指向自身而非 opener；index.html 也没有 message 事件监听
- **修复**:
  1. `wizard_har.html:294`: `window.parent` → `window.opener`（+null guard）
  2. `site_manager.py`: `/api/har/generate` 返回 `site_key` 字段
  3. `index.html`: 添加 `message` 事件监听，收到 `config_saved` 后调用 `loadSites()` + 列表数量验证
  4. `site_manager.py:load_sites()`: 删除 `extract_chains` list 格式兼容代码，统一 dict 格式
  5. `index.html:384`: 删除 `extract_chains` 双格式判断，统一用 `Object.keys()`
- **格式确认**: 所有写入方（HAR wizard、auto_config、config_wizard）和读取方（site_config.py、crawler.py）均使用 dict 格式 `{"video": {"steps": [...]}}`, list 格式是死代码

## I1: screenshot 调试截图集成
- 新增 `_debug_screenshot()` 和 `_cleanup_screenshots()` 到 fast_crawler.py
- 水位线：上限100张，超限删最旧保留50张
- 集成5处截图点：翻页后、点击失败、视频/图集异常
- 已提交 `aac8400`

## BUG2: 配置格式 YAML→JSON + 保存/读取对齐
- **根因**: YAML 格式易出错、调试难，保存和读取经手多个模块格式对齐难保证
- **修复**: 全链路 YAML→JSON
  1. `site_manager.py`: `SITES_YAML→SITES_JSON`, `json.load/dump` 替代 yaml, 删除 `_ensure_yaml()`
  2. `site_config.py`: `json.load` 替代 `yaml.safe_load`
  3. `config_wizard.py`: `write_config()` 改用 `json.dump`
  4. `wizard_har.html`: 展示字段 `data.yaml→data.json`
  5. `index.html`: "查看YAML"→"查看JSON", `d.yaml→d.json`
  6. 删除 `sites.yaml`（旧数据不迁移）
- **验证**: `tmp/test_config_roundtrip.py` 43项全通过（save→read_raw→load_sites→load_site_config→list_available→JSON格式）
- 已提交 `57c866c`

## 配置说明文档
- 创建 `CONFIG_REFERENCE.md`：完整描述 sites.json 每个字段的数据结构、爬取使用位置、生成方式
- 核心发现：crawler.py 消费 extract_chains（逐步执行 JS），fast_crawler.py 不消费 extract_chains（网络捕获替代）
- extract_chains 链名 "video"/"art" 是硬编码映射到 video_links/art_links
- link_patterns.exclude 优先级高于 video_detail/art_detail
- pagination 字段当前不被任何爬取器消费，仅为信息记录

## 爬取架构分析文档（更新版）
- 创建 `ARCHITECTURE_ANALYSIS.md`：完整的架构分析文档，替代之前的 CRAWLER_UNIFICATION.md
- **两大对比**：
  - crawler.py vs fast_crawler.py：配置驱动+JS eval vs 网络优先+启发式，优缺点全面对比
  - auto_config.py vs config_wizard_har.py：全自动6策略 vs 用户录制精确，配置格式差异详解
- **关键发现**：auto_config 生成 `method:"network_capture"` + `filter:".m3u8"` → site_config.py 只读 name+extract_js → **空步骤，配置无效**
- **统一方案**：
  1. 扩展 ExtractStep 支持 4 种 method: eval_js / network_capture / css_selector / interaction
  2. UnifiedCrawler = fast_crawler 架构 + execute_step() 引擎 + SSE 事件 + 截图调试
  3. 两种配置生成器输出统一格式，新增 method 字段
  4. 四阶段实施：数据模型 → 爬虫引擎 → 配置生成 → 迁移清理

## 注意事项
- `crawler/browser_act.py` 有 F4 wait_selector 的未提交改动，需单独提交

## 分组架构重构（P0+P1）
- P0 已提交 (ca42991): site_config.py GroupConfig 数据模型 + site_manager.py 分组格式适配
- P1 已实现待提交: config_wizard_har.py 输出分组格式 + wizard_har.html 分组表单 + index.html 分组显示 + site_manager.py api_add_entry

## 分组入口添加按钮
- index.html 分组卡片新增"＋ 添加入口"按钮（大号、显眼、好点击）
- 点击弹出模态框：显示分组名、URL 输入（必填）、名称输入（可选）
- 调用已有 API `POST /api/sites/<key>/groups/<group_name>/entries` 添加入口
- 添加成功后自动 `loadSites()` 刷新列表更新配置信息
- esc() 函数增强：增加单引号和双引号转义，防止 onclick 属性 XSS

## 分组爬取逻辑实现（P0 续 — group_crawler）
- **新增** `crawler/group_crawler.py`：按 SiteConfig.groups 遍历 entry_points，复用 fast_crawler
- **修改** `site_manager.py`：`/api/crawl/<key>` 改调 `python -m crawler.group_crawler`
- **关键 Bug 修复**：
  1. Python 3.13 的 `sys.path` 不含 `cwd`，subprocess 调用会 `ModuleNotFoundError`
  2. 修复方式：group_crawler.py 顶部 `sys.path.insert(0, _project_root)` + site_manager.py 设置 `PYTHONPATH=BASE_DIR`
  3. `sys.stdout = io.TextIOWrapper(...)` 在 `-m` 模式下会导致 stderr 异常，已移除
  4. **卡住问题**：爬取启动后卡在 "入口 [1/3]" 无后续输出 → 原因是没调用 `ensure_session()` 初始化浏览器会话
  5. **修复**：在 crawl_site_by_groups() 开头调用 `client.ensure_session(first_entry_url, max_wait_cf=90)` 处理 CF 验证+Cookie 复用
  6. **修复**：site_manager.py 传 `--browser-id direct_local_100797252049567848` 复用已有浏览器（与 api_har_start/api_analyze 一致）
- **自测**: `tmp/test_group_crawler.py` 8/8 通过（含 ensure_session + browser-id 验证）
- **状态**: 自测通过，待人工验证后提交
