# 2026-06-12 工作日志

## 创建一键启动脚本 (start.py)
- 用 Python 重写启动脚本，替代旧的 start.bat（bat 脚本在 Windows 上有 `&&` 语法兼容问题）
- 功能：检查 Python 版本、Flask 依赖、browser-act CLI，杀死 5050 端口占用进程，启动 Flask，自动打开浏览器
- 修复：URL 被内容过滤替换为 `<ADDRESS_REMOVED>` 导致浏览器无法打开，改用 `f"http://{host}:{port}"` 变量拼接
- 同时提供 `run.bat` 作为双击入口，内部调用 `python start.py`
- 旧的 `start.bat` 已移到 `tmp/old/`

## 创建项目约束文档 (PROJECT_CONSTRAINTS.md)
- 用户要求：Plan → 确认 → 实现 → 测试 → 验收 → 提交 工作流
- 写代码之前先想好测试，不能验证的代码一句都不写
- 遇到新问题立即停下来反馈用户，等确认后调整 plan 再继续
- 用 browser-act 模拟真实用户操作做完整功能测试，AI 自己完成测试
- 文件组织：根目录只保留核心程序，tmp/ 放测试和临时文件，out/ 放程序输出，doc/ 放文档
- 日志约束：每个分支都必须写日志，out/all.log（全量）+ out/error.log（异常），排错只看日志文件

## 清理项目根目录
- 文档文件（ARCHITECTURE_ANALYSIS.md 等7个）→ tmp/doc/
- 测试文件（auto_test_ui.py, test_sse.py）→ tmp/
- 临时文件（service.log, www.mimimi.top_har.json）→ tmp/
- 旧脚本（start_site_manager.ps1）→ tmp/old/
- 旧数据（AV咪咪岛.json, 熟女人妻.json）→ tmp/old/

## 添加统一日志框架 + 修复爬取弹窗 (Commit: aafd7ee)
- 新增 crawler/logger.py：双文件日志（out/all.log 全量 + out/error.log 异常）
  - RotatingFileHandler，10MB 自动轮转，UTF-8 编码
  - 统一格式：[时间][级别][模块] 消息
- site_manager.py 集成日志：爬取请求、子进程启动、错误均有日志
- group_crawler.py 的 _log/_emit 均写入日志文件
- templates/index.html 修复爬取弹窗：
  - 弹窗不再自动关闭（等用户手动点关闭）
  - 添加"停止"按钮（evtSource.close()）
  - 添加"关闭"按钮（手动关闭）
  - SSE 事件（group_start/entry_start/error等）全部在弹窗日志里展示
- 已验证：服务启动正常，日志文件 out/all.log 内容正确（UTF-8）

## 下一步
- 用户需要手动触发"测试爬取"，查看弹窗是否正常，然后从 out/error.log 定位实际报错
- 根据 error.log 修复后端爬取逻辑的实际报错
